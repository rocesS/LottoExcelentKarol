package pl.lotto.feature;

import org.junit.jupiter.api.Test;

// scheduler, awaiatlity, objectmapper, mvc result pozwala na andExpect(status().isOk())

public class WinningUserLotto extends BaseIntegrationTest {

    @Test
    public void f() {
        // STEP 1 POST /inputNumbers [1,2,3,4,5,6] & got lotteryId, drawDate
//        mockMvc.perform()
        // STEP 2
    }
}
